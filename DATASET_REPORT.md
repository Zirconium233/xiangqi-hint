# 第一批采集数据检查（2026-09-12）

## 输入与质量

- 原始目录：`datasets/raw/20260912_161911_154114`。
- 1,510 张 PNG，1,401,461,228 字节；采集跨度约 562 秒。
- 全部图片可解码，全部为 1320×805。抽样确认包含多局红/黑视角、选中标记、走子、吃子/将军动画、结算和菜单。
- 自动网格质量门槛把 29 帧列入无网格/遮挡待审区，不删除原图。这是启发式筛选，不是人工认证。
- 旧 CNN 的全盘高置信门槛只通过 7 帧，不能用这个门槛丢弃训练素材。

## 标签与产物

`datasets/prepared/batch1_templates/`：

- `labels.jsonl`：9,722 个唯一裁片，含来源、坐标、模板分数与标签。
- `occurrences.jsonl`：每个裁片在原始帧中的全部出现位置，支持追溯与分组。
- `crops/`：原始裁片。2,296 个严格模板标签；7,426 个未标注。
- `train.jsonl`：1,685 个训练候选。
- `validation_provisional.jsonl`：551 个验证候选。
- `unlabelled.jsonl`：7,426 个待审样本，不得把 suggestion 字段当真值。
- `accepted_weakest_review.png`：各类别最低分样本拼图，已查看，未发现明显类别错误；并非对全部标签逐一认证。

标签以人工查看确认的第一张标准开局为模板来源，不使用旧 CNN 预测作为训练真值。自动标签要求相关系数 >=0.96、与第二类别差距 >=0.045、颜色像素误差 <=0.025；第一张按标准棋位标注。动画、偏移和模糊样本可能匹配失败，保留未标注；严重遮挡应使用 unknown/occluded，而不是猜棋子。

分组基于抽样确认的对局边界：前两局安全范围 0–430 / 450–1030 为训练，第三局 1070–1490 为临时验证。边界区间不用于训练。跨组完全相同的裁片只进入训练，避免精确重复泄漏。近似重复和模板标签偏差尚可能存在，因此验证集需要独立标签审核，不能视为最终金标准测试集。

## 尚欠缺

- 模板通过样本以空格居多（1,723）；训练集中红马仅 6 个，存在严重不平衡。
- 临时验证仅覆盖 7 类，不足以评估全部 15 类。
- 只有一个窗口尺寸/皮肤，后续应补充不同尺寸、双方颜色与边角样本。
- 建议视觉 API 对未标注裁片结合原图与邻帧进行辅助标注；无法判断时必须返回 unknown，不做强制补全。API 结果同样需要抽查。
- 可以先试训高可信子集，但目前不建议覆盖线上模型。

## 本机训练能力

RTX 4070 Laptop，8GB 显存。`benchmark_training.py` 对随机初始化的同结构 PieceNet 做了 5 次预热和 40 次 FP32 训练迭代，不写模型权重。

- batch=128，40 步约 0.355 秒，约 14,422 样本/秒。
- 峰值 PyTorch 分配显存约 215 MB（不含桌面、驱动与其他进程）。
- 这是常驻 GPU 随机张量的计算基准，不包含读盘、增强、验证；不能直接当实际训练耗时。
- 本批规模预计本机完成短程微调可控制在数分钟内；先使用 CUDA、缓存小图和类别均衡。暂时无需云端凭据。
- 旧 `xiangqi_cnn.py:train` 仅选 MPS/CPU，随机裁片拆分且覆盖原模型；不要直接拿它训练此数据。后续训练应使用 CUDA、上述分组清单、独立输出 checkpoint，并检查逐类和整盘准确率。

## 复现

```powershell
python prepare_dataset.py datasets/raw/20260912_161911_154114 datasets/prepared/new_audit --build
python label_templates.py datasets/raw/20260912_161911_154114 datasets/prepared/new_audit datasets/prepared/new_labels
python export_training_manifest.py datasets/prepared/new_labels
```

模板脚本的第一张标准开局假设和导出脚本的对局范围仅适用于本批；其他批次必须先确认或修改，不能盲用。原始数据未删除、未上传，现用模型未改动。
