# 数据标注、训练与回滚

## 2026-09-12 已完成的模型更新

本轮直接调用本机 dsh 配置中的 Qwen3.8-Flash-Next 接口，没有通过 harness 创建任务。密钥只在内存中读取；保存文件不包含密钥。只发送棋格拼图，不发送头像/完整桌面。

首轮 48 格拼图会发生编号/颜色混淆，结果未用于训练。随后改为每批 12 格、明确 ID、严格校验返回结构。低模板相似度的严重遮挡样本不调用 API。

- 4,261 个裁片完成新版视觉预标注。
- 只采用人工确认的开局标签，或视觉结果与模板一致的标签。
- 训练 2,560 个，按对局划分的验证 968 个。
- 6,125 个裁片仍未标注，保留在 `training_v2/unlabelled.jsonl`，不得直接用 suggestion 字段训练。
- 验证缺少 B/A/K/b/k 五类；额外人工标注的历史中局/残局共 180 格覆盖全部 15 类。这个小回归集不能代替更广泛的独立测试。

采用旧模型微调、CUDA、类别均衡采样、轻微位移/缩放/亮度增强。30 轮训练本体约 8.36 秒；首次验证达到最优的第 1 轮为最终候选。包含加载等开销的命令总耗时稍长。

| 离线检查 | 旧模型 | 新模型 |
|---|---:|---:|
| 验证裁片错误 / 968 | 2 | 0 |
| 验证低置信度（<0.9） | 4 | 1 |
| 人工历史截图错误 / 180 | 0 | 0 |
| 历史截图最低置信度 | 0.728 | 0.999 |
| ±3 像素偏移错误 / 720 | 2 | 1 |

偏移测试仍有一个错误，且新模型可能对错误预测非常自信。以上结果不表示动画/未知界面已能可靠识别，也不保证棋盘定位正确；GUI 对不可信局面的保护仍应保留。验证标签主要来自模板/视觉一致性，存在筛选偏差，不应把 100% 当作实战识别率。

当前安装：`xiangqi_cnn.pt`。

本轮产物目录：`datasets/runs/20260912_170442/`：

- `previous.pt`：替换前的旧权重。
- `candidate.pt`：通过门槛的新权重，与当前安装文件的 SHA256 一致。
- `report.json`：旧/新指标、逐类召回率、错误位置、训练过程和部署结果。

GUI 若已运行，须关闭后重新启动才能加载新权重：

```powershell
python -X utf8 win_hint_gui.py
```

如需回滚，先退出 GUI，再执行：

```powershell
Copy-Item -LiteralPath datasets/runs/20260912_170442/previous.pt -Destination xiangqi_cnn.pt
```

## 脚本

额外的 API 标注依赖：`python -m pip install requests PyYAML`。API 标注依赖本机 `.dsh` 配置，不是开箱即用的公共服务；配置和凭据不包含在仓库中。`datasets/` 下的训练数据、评估集与备份仅存在本机，克隆仓库不会获取这些文件。

```powershell
# 断点续标：仅针对本批已裁剪的数据；不输出密钥。
python annotate_api.py --workers 8
python merge_vision_labels.py
python audit_training_labels.py

# 人工历史截图回归集；绝不用于训练。
python build_gold_eval.py

# 先训练候选，默认不覆盖当前模型。
python train_prepared.py --epochs 30
# 明确需要通过门槛后替换时：
python train_prepared.py --epochs 30 --deploy
```

脚本内的开局模板、对局分组范围与历史截图坐标仅适用于本批。新批次必须重新确认。训练脚本验证准确率、人工回归错误、低置信数量和偏移错误均不退步，且有实际改善时才允许部署。每次成功替换都会备份当时权重。

本次没有启动游戏 GUI、点击或操作焦点；没有上传数据到云端训练机器；没有执行 Git commit/push。
